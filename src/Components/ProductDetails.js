import React from "react";
import Navbar from "./Navbar";
const ProductDetails = ({ cartItems }) => {
  console.log(cartItems);
  return <div>{/* <Navbar cartItems={cartItems} /> */}</div>;
};

export default ProductDetails;
